[project]
name = "fhatha"
version = "0.1.0"
description = ""
dependencies = ["numpy", "requests", "scipy"]  # etc.

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
test = ["pytest"]


[tool.setuptools]
packages = ["fhatha"]
